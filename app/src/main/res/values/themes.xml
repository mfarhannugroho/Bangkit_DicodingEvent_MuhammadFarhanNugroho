<resources>

    <style name="Theme.BangkitDicodingEvent" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Nonaktifkan Action Bar dari dekorasi jendela -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Customize your theme here -->
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="EventDescriptionText" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textSize">14sp</item>
        <item name="android:lineSpacingMultiplier">1.2</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>
</resources>